name = "soc-log-analyzer-ui"
compatibility_date = "2025-08-27"

# Pages configuration
pages_build_output_dir = "out"

[env.production.vars]
# Backend URL for production deployment
# Use the Workers env-suffixed domain (wrangler deploy --env production)
NEXT_PUBLIC_API_URL = "https://soc-log-analyzer-production.stephen-lamari.workers.dev"

# Optional preview environment
[env.preview.vars]
NEXT_PUBLIC_API_URL = "https://soc-log-analyzer.stephen-lamari.workers.dev"